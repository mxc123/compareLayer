1. Run the tool, drag the left border into the final file submitted by the model, and drag the right border into the corresponding texture and rig file (can be two files, or any one)
2. Run the tool and wait for the prompt (matching success, or error)
3. Running results:
1. If the level is different, a configuration file of errorLayer.json will be generated on the desktop (there is no file generated instead)
2. ErrorLayer.json file is stored in the wrong level information (open file modification information)